# Database Configuration
# IMPORTANT: You must provide your own PostgreSQL database
# Replace with your external PostgreSQL connection details
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://user:password@your-database-host:5432/collabio?schema=public"

# Application Port Configuration
# The port on which the application will run
# Change this if port 3000 is already in use
PORT=3000

# NextAuth Configuration
# For production, use your actual domain
NEXTAUTH_URL="http://localhost:3000"

# Generate a secure secret with: openssl rand -base64 32
# IMPORTANT: Change this in production!
NEXTAUTH_SECRET="your-secret-key-change-this-in-production-min-32-characters-long"

# Seed Data (Optional - used when running npm run db:seed)
# If not set, default development credentials will be used
# ADMIN_EMAIL="admin@yourdomain.com"
# ADMIN_PASSWORD="your-secure-admin-password"
# USER_EMAIL="user@yourdomain.com"
# USER_PASSWORD="your-secure-user-password"
